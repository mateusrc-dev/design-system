import { Meta } from "@storybook/addon-docs"; // definindo configuração global - tudo que colocarmos dentro de Meta vai fazer parte da configuração default - definir documentação
import { TokensGrid } from "../../components/TokensGrid";
import { fontSizes } from "@mateus-ignite-ui/tokens"
// toda vez que colocamos uma barra é criado um submenu no storybook

<Meta title="Tokens/Font Sizes" />

# Font Sizes

Essas são os font-sizes utilizadas no Ignite UI.

<TokensGrid tokens={fontSizes} hasRemValue={true} />